[pytest]
addopts = -q
pythonpath = src
testpaths = tests
markers =
    integration: requires FINA_TEST_DATABASE_URL pointing to a PostgreSQL test server
    e2e: builds and runs two application containers; enable with --run-e2e
